[project]
name = "mcp-claudectl"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "mcp[cli]>=1.3.0",
  "httpx>=0.27",
  "starlette>=0.40",
  "uvicorn[standard]>=0.30",
]

[tool.uv]
package = false
